// Copyright 2026 pejman-pour-moezzi. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAdsetsPromotedCmd(flags *rootFlags) *cobra.Command {
	var flagFields string
	var flagDatePreset string
	var flagBreakdowns string
	var flagActionAttributionWindows string

	cmd := &cobra.Command{
		Use:     "adsets <adset_id>",
		Short:   "Ad set insights with attribution dedup",
		Long:    "Shortcut for 'adsets insights'. Ad set insights with attribution dedup",
		Example: "  meta-ads-pp-cli adsets",
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/{adset_id}/insights"
			if len(args) < 1 {
				return usageErr(fmt.Errorf("adset_id is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "adset_id"))
			}
			path = replacePathParam(path, "adset_id", args[0])
			params := map[string]string{}
			if flagFields != "" {
				params["fields"] = fmt.Sprintf("%v", flagFields)
			}
			if flagDatePreset != "" {
				params["date_preset"] = fmt.Sprintf("%v", flagDatePreset)
			}
			if flagBreakdowns != "" {
				params["breakdowns"] = fmt.Sprintf("%v", flagBreakdowns)
			}
			if flagActionAttributionWindows != "" {
				params["action_attribution_windows"] = fmt.Sprintf("%v", flagActionAttributionWindows)
			}
			data, prov, err := resolveRead(c, flags, "adsets", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagFields, "fields", "adset_id,adset_name,spend,conversions,purchase_roas,actions,action_values,clicks,impressions,reach,frequency,cpm,cpc,ctr", "Fields")
	cmd.Flags().StringVar(&flagDatePreset, "date-preset", "last_14d", "Date preset")
	cmd.Flags().StringVar(&flagBreakdowns, "breakdowns", "", "Breakdowns")
	cmd.Flags().StringVar(&flagActionAttributionWindows, "action-attribution-windows", "7d_click,1d_view", "Attribution windows")

	// Wire sibling endpoints and sub-resources as subcommands
	cmd.AddCommand(newAdsetsCreateCmd(flags))
	cmd.AddCommand(newAdsetsDeleteCmd(flags))
	cmd.AddCommand(newAdsetsGetCmd(flags))
	cmd.AddCommand(newAdsetsListByAccountCmd(flags))
	cmd.AddCommand(newAdsetsListByCampaignCmd(flags))
	cmd.AddCommand(newAdsetsUpdateCmd(flags))

	return cmd
}
